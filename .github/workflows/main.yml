name: MainWorkflow

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: self-hosted

    steps:
      - name: Check out the repository
        uses: actions/checkout@v2

      - name: Print Hello World
        run: echo "Hello, World!"
